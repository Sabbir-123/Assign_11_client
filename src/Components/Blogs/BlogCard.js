import React from 'react';

const BlogCard = ({blogcard}) => {
    const {title, details} = blogcard
    return (
        <div>
          
         
        </div>
    );
};

export default BlogCard;


